# Project and dataset names are public identifiers.
PUBLIC_SANITY_PROJECT_ID=th4gij3b
PUBLIC_SANITY_DATASET=production

# Server/build-time only. Never prefix this token with PUBLIC_.
SANITY_API_READ_TOKEN=

# Production and ordinary builds use Sanity. Select markdown only explicitly for local fallback tests.
CONTENT_SOURCE=sanity
