# The project and dataset are public identifiers; no read token is required.
PUBLIC_SANITY_PROJECT_ID=th4gij3b
PUBLIC_SANITY_DATASET=production

# Keep Markdown as the rollback source until the Sanity parity gate is approved.
CONTENT_SOURCE=markdown
