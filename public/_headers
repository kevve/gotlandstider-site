# Keep Cloudflare staging and preview hostnames out of search results. This host
# pattern does not match the production custom domain.
https://:version.:subdomain.workers.dev/*
  X-Robots-Tag: noindex

# Astro fingerprints generated assets, so repeat visitors can cache them safely.
/_astro/*
  Cache-Control: public, max-age=31556952, immutable
